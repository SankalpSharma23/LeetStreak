# Dependencies - INTENTIONALLY TRACKED for users to run extension immediately
# node_modules/ is tracked so users don't need to run npm install
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# NOTE: dist/ is intentionally NOT ignored - it's pre-built for users to load directly
# Users can clone and immediately load the extension without npm install

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/

# Temporary folders
tmp/
temp/

# Optional REPL history
.node_repl_history

# Extension packaging
*.crx
*.pem